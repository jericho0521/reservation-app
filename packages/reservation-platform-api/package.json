{
  "name": "@reservation-platform/api",
  "version": "0.0.0",
  "description": "Framework-neutral reservation platform API adapters and compatibility services.",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "license": "UNLICENSED",
  "author": "Project Play maintainers",
  "repository": {
    "type": "git",
    "url": "https://github.com/jericho0521/reservation-app.git",
    "directory": "packages/reservation-platform-api"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepack": "pnpm run build",
    "test": "tsc -p tsconfig.build.json && node --import tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@project-play/reservation-chat-core": "workspace:*",
    "@project-play/reservations-core": "workspace:*",
    "@reservation-platform/contract-types": "workspace:*",
    "@node-rs/argon2": "^2.0.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
