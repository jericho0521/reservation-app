# @reservation-platform/api

Framework-neutral API adapter helpers for the reservation platform compatibility
track.

This package is a backend-owned extraction step. It contains DTO mapping,
metadata, and error payload helpers that can move toward a standalone backend
API without importing Next.js route primitives, React UI, frontend pages, or
browser Supabase helpers.
