import type { ServiceResponse } from "@reservation-platform/sdk";

export function ReservationFilters({ services, values }: { services: ServiceResponse[]; values: { q?: string; status?: string; channel?: string; service?: string } }) {
  return <form className="reservation-filters" method="get"><label>Search<input name="q" defaultValue={values.q ?? ""} placeholder="Customer or booking ID" /></label><label>Status<select name="status" defaultValue={values.status ?? ""}><option value="">All</option><option value="confirmed">Confirmed</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select></label><label>Channel<select name="channel" defaultValue={values.channel ?? ""}><option value="">All</option><option value="web_booking">Web booking</option><option value="web_chat">Web chat</option><option value="whatsapp">WhatsApp</option><option value="simulation">Simulation</option></select></label><label>Service<select name="service" defaultValue={values.service ?? ""}><option value="">All</option>{services.map((service) => <option key={service.service_id} value={service.service_id}>{service.name}</option>)}</select></label><button type="submit">Apply filters</button></form>;
}
