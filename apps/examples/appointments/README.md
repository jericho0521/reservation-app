# Appointments example

Set `NEXT_PUBLIC_RESERVATION_PLATFORM_BASE_URL` to the standalone API. The app
uses the published `luma-studio` experience by default; override it with
`NEXT_PUBLIC_RESERVATION_EXPERIENCE_SLUG`.
