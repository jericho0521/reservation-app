# @reservation-platform/platform-config

Strict JSON runtime manifest parser for backend module selection.

Use `RESERVATION_PLATFORM_CONFIG_PATH` to point the standalone backend at a
deployment manifest. Secrets remain in environment variables.
