# Remaining Modularity Gaps

This index answers whether the known remaining modularity work is captured in
the phase docs. It does not replace the phase plans; it points each gap to the
phase that owns it so subagents can work from one phase file without losing the
overall outcome.

The focused execution plan for proving hard frontend/backend separation lives in
[Frontend and Backend Hard Separation Plan](frontend-backend-hard-separation-plan/README.md).
Use it when the question is whether the backend platform, SDK, and frontend are
actually separable products rather than modular packages inside this monorepo.

The focused execution plan for finishing physical product separation lives in
[Frontend and Backend Physical Separation Plan](frontend-backend-physical-separation-plan/README.md).
Use it when assigning subagents to turn the current modular monorepo readiness
into a backend product repository, SDK artifact, frontend consumer repository,
live backend proof, external adoption proof, and compatibility cleanup.

The concise repo-first execution pack lives in
[Backend Product, SDK, and Frontend Separation Plan](backend-product-sdk-frontend-separation-plan/README.md).
Use it when subagents need phase files for the intended product model: the
backend GitHub repository is the platform product, the SDK is the installable
contract, and every frontend is a replaceable consumer.

The focused current-state execution pack lives in
[Current Refactor Separation Gap Plan](current-refactor-separation-gap-plan/README.md).
Use it when the task is to answer whether the current branch is actually
frontend/backend separated yet, then close the remaining gaps without relying on
chat history.

The latest direct answer pack lives in
[Current Separation Answer Plan](current-separation-answer-plan/README.md).
Use it when a subagent needs the shortest phase-by-phase path from "the branch
is modular but not product-separated yet" to backend product boundary
enforcement, SDK install contract enforcement, frontend consumer detachment
proof, live cross-repo proof, and compatibility cleanup/release decision.

The current frontend/backend separation plan lives in
[Current Frontend and Backend Separation Plan](current-frontend-backend-separation-plan/README.md).
Use it when a subagent needs the latest focused phase pack for the current
state: partial separation now, then backend product boundary closure, SDK
install contract, frontend consumer detachment, live external proof, and
compatibility cleanup/release decisions.

The backend platform product completion plan lives in
[Backend Platform Product Completion Plan](backend-platform-product-completion-plan/README.md).
Use it as the current execution pack for the product intent: backend GitHub
repository as the infrastructure product, SDK as the installable contract, and
frontend as a replaceable consumer. It is organized for subagents to close the
remaining product gaps phase by phase while updating downstream phases whenever
ownership, API, env, proof, or compatibility assumptions change.

## Gap Ownership

| Remaining gap | Owning phase doc | Current status |
| --- | --- | --- |
| Extract platform route handlers from Next.js route glue into backend-owned application services. | [Phase 1 Boundary Results](phase-1-backend-module-boundary-results.md) | Explicitly documented. Availability query preparation, repository read orchestration, slot generation, metadata/default-label handling, DTO shaping, and storage error result shaping now live in `@reservation-platform/api` behind an injected availability repository port. Catalog list/read orchestration for venues, services, resources, and resource layouts now runs through a framework-neutral `@reservation-platform/api` catalog request dispatcher behind an injected `PlatformCatalogRepository`; the current Next compatibility files wire the repository factory and JSON serialization only. Protected-route host Supabase auth preflight is centralized in `/api/v1` route utilities, and reservation list/read-by-id plus create/update/cancel/reschedule mutation orchestration now live in framework-neutral `@reservation-platform/api` application-service functions backed by injected repository ports. The Next compatibility files now own auth preflight, storage package factory wiring, create repository construction, resource-id label resolution for create/reschedule preparation, and JSON serialization for those paths. Resource-maintenance list/create/end orchestration also lives in framework-neutral package services, and `apps/api` now routes the standalone `GET /v1/resource-maintenance`, `POST /v1/resource-maintenance`, and `POST /v1/resource-maintenance/{id}/end` contract through an injected `ResourceMaintenanceRepositoryPort`. Reservation read and update-style mutation Supabase adapters now live in `@project-play/reservations-supabase`; auth/tenant enforcement and live standalone backend rollout remain incomplete. |
| Replace compatibility booking/seat naming with generic reservation/resource service contracts. | [Phase 1 Boundary Results](phase-1-backend-module-boundary-results.md) | Partially implemented. Public `@reservation-platform/api` reservation service error messages now use generic reservation/resource language for create validation, atomic capacity/resource conflicts, list/read failures, update failures, and cancel failures. Resource conflict/capacity details now include generic `resource_labels` and `available_quantity` aliases while preserving legacy `seat_labels` and `available_seats` for compatibility consumers. Legacy storage fields, compatibility adapter internals, fixtures, and DB naming remain until the final DB/API migration. |
| Move database access behind backend repository interfaces. | [Phase 1 Boundary Results](phase-1-backend-module-boundary-results.md) | Partially implemented. The current `/api/v1` catalog shim now wires `createSupabasePlatformCatalogRepository()` from `@project-play/reservations-supabase`, preserving the anon-client venue/service reads and service-role resource/layout reads while moving table/select/order/filter logic into the backend storage adapter package. Catalog endpoint matching, id decoding, `service_id` resource filtering, missing-repository failure shaping, and catalog DTO/error result shaping now live in `@reservation-platform/api`; Next.js route files only pass the request/path into shared serialization glue. A source-level tenant/venue repository adapter proof also exists there. `/api/v1/availability` now injects a neutral `AvailabilityRepositoryPort` into `@reservation-platform/api`; the local factory still wires `createSupabaseAvailabilityRepository()`, while the package service owns query validation, storage read orchestration, slot generation, metadata/default-label handling, platform DTO shaping, and platform error result shaping. Resource-maintenance list/create/end storage now delegates active-row listing, resource lookup, service lookup, legacy maintenance upsert, and end-update table/select details to `createSupabaseResourceMaintenanceRepository()`, and framework-neutral `@reservation-platform/api` service functions own the policy preparation, repository error classification, and DTO response shaping through a repository port. Reservation list/read-by-id orchestration now uses a framework-neutral `ReservationReadRepositoryPort`, and `createSupabaseReservationReadRepository()` owns the `bookings` `*, services(name)` select, descending `booking_date` order, search filter application, search-only limit, id equality, and `.single()` query shape in the storage package. Reservation update/cancel/reschedule mutation orchestration now uses a framework-neutral `ReservationMutationRepositoryPort`; package services own UUID validation, legacy update patch validation, `updated_at` stamping, cancel status construction, repository error classification, and platform DTO/error response shaping, while `createSupabaseReservationMutationRepository()` owns the authenticated Supabase `bookings.update(patch).eq("id", reservationId).select().single()` query shape in the storage package. Reservation create orchestration now uses a framework-neutral `ReservationCreateRepositoryPort`; the package owns legacy create validation, legacy booking-to-reservation conversion, atomic error classification, and platform DTO/error body shaping while current app compatibility glue wires `createSupabaseReservationRepository(supabaseAdmin())`. Reservation create/reschedule compatibility shims resolve resource ids to labels through `createSupabaseReservationResourceLabelRepository()` instead of spelling the `reservable_resources` label lookup in the route helper. `apps/api` now has a backend-only runtime factory that can wire catalog, availability, reservation read/create/mutation, resource-maintenance, idempotency, and tenant/venue context repositories from `RESERVATION_SUPABASE_URL`, `RESERVATION_SUPABASE_ANON_KEY`, and `RESERVATION_SUPABASE_SERVICE_ROLE_KEY` without importing current-app Supabase helpers. Live route-wide adoption, live DB/RLS proof, deploy proof, and final standalone backend extraction remain incomplete. |
| Add bearer-token-aware platform auth and tenant/venue enforcement. | [Phase 4 Auth and Config Split Results](phase-4-auth-tenant-runtime-config-split-results.md) | Partially implemented. Current frontend and SDK forward context headers, and `@reservation-platform/api` now parses them framework-neutrally, provides framework-neutral auth/tenant decision helpers for already-verified principals, includes a provider-neutral `principalFromTokenClaims()` mapper for decoded token claims, and includes a repository-backed tenant/venue validation helper. `@project-play/reservations-supabase` now has a source-proofed `createSupabaseTenantVenueRepository(client)` adapter that reads the planned `tenants` table and selects `id, tenant_id` from `venues` for fail-closed ownership validation. The current `/api/v1` route utility layer now has a backend-only `RESERVATION_PLATFORM_SERVICE_API_KEY` service-token preflight adapter that consumes `Authorization: Bearer ...` through the package parser and can reject before body parsing/idempotency mutation work; existing protected mutation shims now centralize their current host Supabase cookie/session preflight there too. Admin client components now use `lib/admin-auth-client.ts` for sign-in/sign-out only, and current-frontend boundary scans block direct raw Supabase browser helper imports outside that app-owned auth facade. Current protected reservation/resource-maintenance mutation shims validate required `X-Reservation-Tenant-Id` plus optional `X-Reservation-Venue-Id` through the Supabase tenant/venue repository after current host auth and before body parsing/mutation execution, and reservation read/list compatibility shims now run the same host-auth tenant/venue validation before reservation read storage work. `apps/api` now separately supports optional standalone backend service-token protection and provider-neutral JWT/JWKS bearer verification from standalone runtime env for catalog, availability, reservation, resource-maintenance data routes, and optional chat routes. The service token remains a server-to-server bypass when it exactly matches; non-service bearer tokens can be verified through the JWT/JWKS verifier. Verifier principals are fed into the shared tenant/venue/role/scope authorization helpers, verifier failures return stable platform errors, thrown verifier errors fail closed without provider details, and auth still runs before route repository, idempotency, protected mutation body work, disabled chat fallback, or injected chat module calls. Metadata remains intentionally public. This proves standalone provider-neutral JWT/JWKS verification, bounded JWKS cache/unknown-`kid` refresh behavior, authorization plumbing, decoded-claim mapping, and local disabled-chat auth readiness. Live provider configuration, provider operational key rotation, live DB migration/application proof, tenant/RLS proof, broad live route wiring, deployment, live optional-chat behavior, and final standalone backend extraction remain missing. |
| Add idempotency persistence, replay, and misuse protection in the backend. | [Phase 4 Auth and Config Split Results](phase-4-auth-tenant-runtime-config-split-results.md) and [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Partially implemented. SDK/current frontend forward `Idempotency-Key`, backend context parsing can read it, and `@reservation-platform/api` now has a framework-neutral idempotency repository port with atomic claim semantics, canonical JSON fingerprint helper, proceed/replay/misuse/in-progress decision semantics, and commit helper. Current reservation and resource-maintenance `/api/v1` compatibility mutation shims now use route-level begin/replay/commit wiring backed by a backend-runtime resolver that selects the durable Supabase idempotency repository when service-role config is present and falls back to in-process local/dev/test compatibility storage when config is absent. `apps/api` now proves idempotent `POST /v1/reservations`, reservation lifecycle mutation host wiring, `POST /v1/resource-maintenance`, and `POST /v1/resource-maintenance/{id}/end` with an injected `IdempotencyRepository`, required `Idempotency-Key`, malformed-JSON preparse rejection when the key is missing, completed-response replay, key-misuse rejection, and no completed idempotency record for invalid maintenance bodies using fake repositories. Its backend-only Supabase runtime factory wires `createSupabaseIdempotencyRepository()` when complete standalone Supabase config is present. `@project-play/reservations-supabase` now has a backend-only Supabase idempotency repository adapter, and `packages/database/migrations/supabase/000011_platform_idempotency.sql` now exists as the concrete package-owned bundle asset for the `platform_idempotency_records` table plus durable idempotency claim/store RPCs. Disposable database proof, migration application, tenant/RLS proof, live durable idempotency behavior, and live standalone rollout remain incomplete. |
| Keep SDK frontend-safe and HTTP-only. | [Phase 2 SDK Boundary Results](phase-2-sdk-boundary-public-client-results.md) | Explicitly documented and partially implemented. Package boundary scans exist, and `sdk:registry-install-proof` now adds CI-safe private/public/disposable registry install readiness with a unit-tested env parser. It skips without install opt-in by default and never publishes. The disposable strict registry proof has passed once with exact SDK and contract package version specs from a temporary local registry; public/private registry pilot proof remains separate. |
| Make the current frontend a consumer of the backend API/SDK instead of backend owner. | [Phase 3 Frontend API Migration Results](phase-3-frontend-api-migration-results.md) | Explicitly documented and partially implemented through `lib/reservation-platform-client.ts`. The server-rendered `/admin` initial reservation load now calls a server loader that delegates to the wrapper instead of querying `bookings` directly; platform mode now uses the optional `GET /v1/reservations` summary contract for exact `confirmed_today` counts when available and falls back to deriving from returned bookings only for compatibility responses without summary. Some local compatibility routes remain intentionally in place. |
| Cut the current frontend over so it behaves like any external consumer frontend. | [Phase 8 Current Frontend Consumer Cutover](phase-8-current-frontend-consumer-cutover.md) | Partially implemented. `lib/reservation-platform-client.ts` now has browser-safe `NEXT_PUBLIC_RESERVATION_PLATFORM_BASE_URL` configuration so platform-mode service, availability, reservation create/status update, admin reservation list, and resource-maintenance list/save calls use `@reservation-platform/sdk` against standalone `/v1` paths when an absolute backend origin is configured while preserving empty-env and non-absolute-env relative `/api/v1` compatibility behavior through the direct HTTP fallback. `lib/reservation-platform-client.test.ts` now includes a CI-safe local proof that configured platform mode fails if those reservation-platform calls fall back to relative `/api` routes or the current frontend origin's `/api/v1` compatibility routes, and it covers SDK query serialization so configured standalone calls do not silently regress to the older hand-built URL branch. `lib/reservation-chat-client.ts` now uses the same browser-safe base URL setting for platform chat session creation, message, and confirmation calls, sending configured absolute origins to `/v1/chat/...` without doubling `/v1`; `lib/reservation-chat-client.test.ts` fails if configured platform chat falls back to `/api/v1`, while empty or non-absolute config keeps the compatibility fallback. Local mode remains on temporary `/api/*` compatibility routes and ignores the platform base URL. In platform mode, the public platform base URL takes precedence over `listAdminReservations(input.baseUrl)` only when it is absolute because SSR compatibility passes the current frontend origin there; when the public env is absent or non-absolute, `baseUrl` still targets `${baseUrl}/api/v1/reservations` for current-app compatibility, while explicit standalone callers can pass an absolute `platformBaseUrl` to use the SDK against `/v1/reservations`. `current-frontend:platform-smoke` now starts a local mock standalone backend on a separate `127.0.0.1` origin, configures the Next dev server with that origin through `NEXT_PUBLIC_RESERVATION_PLATFORM_BASE_URL`, drives the existing form-booking browser flow, handles CORS preflight, verifies services/availability/create headers and payloads, and fails on current-frontend `/api`, `/api/v1`, or mock-backend `/api` compatibility usage. `current-frontend:admin-platform-smoke` now uses the same external-origin mock-backend pattern for admin reservation list/search, complete/cancel/restore, services, and resource-maintenance list/create/end flows; it tracks browser-observed non-OPTIONS standalone `/v1` calls separately from backend-handled calls and fails on current-frontend `/api`, current-frontend `/api/v1`, or mock-backend `/api` compatibility usage. The current frontend platform boundary and secret scans now derive scan roots from every `include` source area in `frontend-consumer-repo-inventory.json` while preserving their broader migrated/admin/platform-smoke reference targets, and a focused helper test guards that include-path coverage. The frontend consumer repo readiness proof now includes the browser-safe chat transport wrapper and legacy response mapping, while keeping `app/chat-booking` and `components/chat` reference-only. Its OS-temp generated package metadata also includes frontend-only `typecheck`, `build`, and `start` scripts, copies an exact pinned source-root `pnpm@x.y.z` `packageManager`, rejects missing/blank/ranged/latest/workspace/non-pnpm/whitespace-padded package-manager values, rewrites SDK-facing package dependencies away from root `workspace:*` specs to deterministic `0.0.0` placeholders, rejects generated `dependencies` or `devDependencies` specs using `workspace:`, `file:`, `link:`, or `portal:`, verifies materialized source external imports are present in generated `dependencies` or `devDependencies` including scoped `@scope/name` derivation and Node built-in allowances, rejects materialized workspace-root metadata such as `pnpm-workspace.yaml` or `turbo.json`, and validates those script commands stay free of backend-platform, database, SDK release/registry, package-workspace, current-frontend proof/smoke, compatibility-route, Supabase, monorepo verifier, and `pnpm --filter` fragments without executing them. The same OS-temp candidate now writes and validates a generated root `tsconfig.json` with DOM libs, strict/noEmit, bundler module resolution, React JSX, `@/*` mapped to `./*`, source includes for `**/*.ts`, `**/*.tsx`, and `**/*.mts`, and only `node_modules` excluded; it rejects `extends`, project references, composite output settings, `outDir`, `rootDirs`, `.next` includes, backend/current-app server paths, backend package names, `packages`/`apps` path segments or aliases, extra `compilerOptions.paths` aliases, absolute paths, and `..` traversal after normalizing Windows-style backslash path separators. `current-frontend:consumer-install-proof` / `current-frontend:consumer-install-proof:strict` now add a prepared-root frontend consumer install/build proof harness: default mode validates only env and prepared-root metadata without installing or executing generated frontend commands, while strict mode requires a prepared frontend workspace outside this repo, exact generated `typecheck`/`build`/`start` scripts, `package.json`, `pnpm-lock.yaml`, no `workspace:`/`file:`/`link:`/`portal:` dependency specs in install-relevant metadata, and `CURRENT_FRONTEND_CONSUMER_PROOF_ALLOW_INSTALL=1` before running only `corepack pnpm install --frozen-lockfile --ignore-scripts`, `corepack pnpm run typecheck`, and `corepack pnpm run build`; install lifecycle scripts are disabled and it never runs `start`, a dev server, or browser checks. This is current-frontend SDK targeting, local mock external-origin browser proof, chat transport wrapper readiness, generated package/tsconfig metadata and import-closure portability readiness, local scan-readiness, and a bounded prepared-root proof harness only; full chat UI extraction, full live `/v1` standalone cutover, compatibility route removal, live enabled chat, live parity/auth/idempotency proof, strict prepared frontend install/build now passes once against staged SDK artifacts, while separated frontend runtime smoke, registry install proof, and complete frontend repo split remain open until the later gates pass. |
| Move LangChain/provider/retrieval workflow behind backend chat services. | [Phase 5 AI Chat Split Results](phase-5-ai-chat-workflow-split-results.md) | Partially implemented. SDK chat namespace and disabled/enabled fixture proofs exist, the current `/api/v1` compatibility surface now returns provider-free `chat_module_disabled` responses for the contracted disabled chat endpoints including the portable `messages:stream` route representation, and the standalone backend extraction manifest now explicitly plans `packages/ai-chat` as the backend-owned optional chat package while keeping `packages/reservation-chat-core` as reference-only migration context. `apps/api` now has an optional provider-neutral `StandaloneApiChatModule` injection boundary for `POST /v1/chat/reservation-sessions`, `/messages`, `/messages:stream`, and `/confirm`; absent injection preserves the exact disabled platform error body when no standalone auth is configured, configured service-token/JWT auth rejects before the disabled fallback, injected fake modules receive tenant/venue/auth/correlation/idempotency request context only after auth succeeds, enabled responses can flow through all four routes, and thrown or returned module errors are sanitized. `@reservation-platform/ai-chat` now includes bounded runtime tenant-scope validation that trims `tenant_id` / optional `venue_id` and fails closed with a public `bad_request` before provider, retriever, checkpoint, or audit ports are called when scope is missing or blank. Real provider adapters, live retrieval, checkpoint persistence, live chat configuration, durable tenant-isolation enforcement beyond request-scope validation, deployment wiring, and seeded live backend parity remain missing. |
| Prove a clean external frontend can install/use the SDK against `/v1`. | [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Explicitly documented and partially proven with fixtures. The live parity harness now exports a unit-tested parser for trimmed env values, required config, strict/env readiness, malformed URL/date/quantity handling, explicit disabled/enabled chat mode, and mutation opt-in decisions. Strict live parity readiness covers reservation create/read/list idempotency replay, resource-maintenance list/create/end SDK/direct HTTP replay, and disabled-chat SDK/direct HTTP error parity for create-session, send-message, stream-message, and confirm-reservation when strict env, mutation opt-in, and `RESERVATION_PLATFORM_LIVE_CHAT_MODE=disabled` are configured. `enabled` chat mode remains an explicit unsupported/pending failure rather than a pass. Live seeded backend parity is still unproven until that strict command passes against disposable seeded backend data; package publication proof also remains missing. |
| Remove local compatibility API routes. | [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Explicitly documented as blocked until backend parity, frontend migration, and external proofs pass. |
| Remove or deprecate current `app/api/**` reservation compatibility routes after cutover. | [Phase 9 Compatibility Route Removal](phase-9-compatibility-route-removal.md) | Partially implemented as readiness infrastructure only. `compatibility-route-inventory.json` now inventories current reservation-platform compatibility routes, optional legacy `/api/chat` plus `/api/v1/chat/**` module routes, and app-owned analytics/blog/update routes separately. `compatibility-route-removal-decision-log.md` now locally documents rollback/deprecation decisions for every non-app-owned compatibility route family and explicitly excludes app-owned analytics/blog/update routes from reservation-platform removal scope. `backend-platform:verify-compatibility-route-removal-gate` is a CI-safe local verifier that enumerates current `app/api/**/route.ts` files, enforces full inventory coverage, checks listed files exist, requires removal/deprecation candidates to have `/v1` standalone equivalents, statically verifies those non-null reservation-platform/optional-module equivalents against actual `handleStandaloneApiRequest` dispatch or explicit `apps/api/src/routes.test.ts` route invocations, requires the legacy `/v1/chat/reservation-sessions/**` claim to cover session/messages/stream/confirm in dispatcher source plus route tests, verifies rollback/deprecation decision-log coverage for non-app-owned routes that claim that gate, requires non-app-owned routes with that gate still false to keep a missing-notes blocker, prevents app-owned routes from being marked for reservation-platform removal, requires the top-level gate list and each reservation-platform/optional-module route to include `current-frontend:consumer-install-proof:strict` and `backend-platform:extracted-install-proof:strict` booleans, requires missing strict proof blockers while they are false, rejects `removable` unless every required gate boolean is true, and runs a bounded migrated-frontend source scan that blocks direct reservation compatibility route literals outside `lib/reservation-platform-client.ts` and rejects stale inventory blockers claiming that direct frontend source scan is not recorded. A bounded local frontend fallback cleanup proof also exists for configured platform-mode reservation-platform client URLs, and the strict extracted backend install/build/test proof and strict current-frontend prepared-artifact install/build proof have now passed once against external prepared roots, but no app/api routes have been deleted, deprecated, moved, or marked removable; actual removal remains blocked until broader current frontend cutover, SDK/direct parity, auth/tenant/idempotency enforcement, live/parity tests, registry proof, deployment proof, and route-level tests pass; safe skipped readiness checks do not count as route-removal proof. |
| Split the backend platform into its own repository or deployable service. | [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Partially implemented as a bounded skeleton proof. `apps/api` is now a private backend-only workspace package with framework-neutral route handlers, an optional Node HTTP server, tests for cheap public health aliases `GET /healthz` and `GET /v1/health`, `GET /v1/metadata`, catalog `GET /v1/venues`, `/v1/services`, `/v1/resources`, `/v1/resource-layouts/{id}` through an injected `PlatformCatalogRepository`, `GET /v1/availability` through an injected `AvailabilityRepositoryPort`, read-only `GET /v1/reservations` and `GET /v1/reservations/{id}` through an injected `ReservationReadRepositoryPort`, idempotent `POST /v1/reservations` through injected `ReservationCreateRepositoryPort` plus `IdempotencyRepository`, idempotent lifecycle mutations through injected `ReservationMutationRepositoryPort` plus `IdempotencyRepository`, resource-maintenance list through an injected `ResourceMaintenanceRepositoryPort`, idempotent resource-maintenance create/end through injected `ResourceMaintenanceRepositoryPort` plus `IdempotencyRepository`, optional service-token/context enforcement plus provider-neutral JWT/JWKS bearer verification for protected data routes, optional provider-neutral injectable chat module routing for `/v1/chat/reservation-sessions/**` with exact disabled fallback behavior, missing repository behavior, backend-only Supabase runtime config validation/wiring with fake-client tests including tenant/venue repository wiring, and source-boundary scans proving it avoids frontend/Next/React/browser Supabase/LangChain/provider/current-app wrapper imports. The health aliases return stable public status/API/service metadata without auth, tenant headers, idempotency, repositories, Supabase env, or request-body parsing, and they remain unprotected when service-token/JWT auth is configured. This is deployability/readiness hygiene only, not live deployment proof, live Supabase connectivity, DB/RLS parity, or live reservation parity. `packages/database` now also exists as a private database bundle package with concrete verified core schema, tenant/auth, resource-maintenance, availability-rule, atomic reservation RPC, RLS, idempotency migration target files, a generated migration-index checksum/apply-plan artifact for extraction, DB-client-neutral TypeScript helpers/contracts for selecting core, optional AI retrieval, and development seed migration plans from that index, and a package/root CI-safe `database:live-proof` harness that can apply the backend-owned migration-index plan to a disposable PostgreSQL database when env plus `psql` are configured. The standalone extraction manifest/dry run now explicitly includes both `packages/ai-chat` and `packages/database` as direct backend package candidates while older root/package SQL files remain reconciliation/reference inputs mapped by the SQL inventory and migration bundle manifests rather than dry-run copy sources. This is not live reservation parity, durable DB-backed idempotency proof against a real database, live provider configuration, provider operational key rotation, a completed database migration/RLS behavior proof, deployment, or an actual separate repository extraction. |
| Make `apps/api` the real standalone backend cutover target. | [Phase 7 Standalone Backend Cutover](phase-7-standalone-backend-cutover.md) | Partially implemented. `backend-platform:verify-standalone-deployment-config` now provides a CI-safe standalone backend deployment/runtime config verifier that parses env only, performs no network calls or deploys, skips when unconfigured, and exports a unit-tested parser. The strict variant fails closed when required standalone deployment env is absent or malformed. It validates optional `PORT`, complete-or-absent backend-only Supabase env, optional nonblank service-token auth, complete-or-absent JWT/JWKS auth env with valid numeric settings, backend-only AI/chat provider env when present, and rejects `NEXT_PUBLIC_*` backend secret-style names. The verifier also includes a local runtime/deployment env-name drift guard against `apps/api/src/runtime.ts` for backend runtime-owned Supabase, `RESERVATION_PLATFORM_SERVICE_API_KEY`, and `RESERVATION_PLATFORM_AUTH_*` JWT/JWKS names; AI/chat provider env names are explicitly allowed as verifier-only deployment-readiness names because runtime does not read them yet. `apps/api/src/server.ts` now exposes a testable backend-only env bootstrap helper for creating the standalone Node server from env-derived dependencies; local tests prove `/v1/health` starts without Supabase env or client creation, service-token-only protected routes fail before repository work, and partial Supabase env throws `StandaloneSupabaseConfigError` before a server is exposed. A real local standalone `apps/api` process has now passed `backend-platform:live-proof:strict` for `/v1/health` outside the current Next.js frontend runtime. `backend-platform:verify-compatibility-route-removal-gate` now adds a local route-surface proof that claimed `/v1` equivalents in the compatibility inventory are represented by actual standalone dispatch or explicit `apps/api` route-test invocation, including dynamic `{id}` patterns and the bounded chat session family. `apps/api` route tests also prove configured service-token/JWT auth protects disabled and injected optional chat routes before disabled fallback or chat module calls, while no-auth disabled fixtures keep the exact existing 404 body. `backend-platform:verify-extraction-boundary` now exports a reusable verifier and focused unit tests prove `apps/api/src` rejects React/Next imports, browser Supabase helpers, frontend platform wrappers, and current app page/admin/chat/form imports. This is readiness, env-name drift hygiene, local route-surface proof, health-only live proof, and source-boundary unit proof; it is not DB-backed live route parity, live Supabase/PostgREST connectivity, provider chat configuration, live optional-chat behavior, final standalone cutover, seeded parity, or final repository extraction proof. The next blocker is that the standalone runtime currently expects Supabase HTTP client config while the disposable proof database is raw PostgreSQL; broader DB-backed protected-route behavior and downstream consumer cutover remain in scope for the rest of Phase 7/8. |
| Prove the separated backend platform against live disposable infrastructure. | [Phase 10 Live Platform Proof](phase-10-live-platform-proof.md) | Partially implemented as bounded health proof plus readiness infrastructure only. `backend-platform:live-proof` / `backend-platform:live-proof:strict` now target `RESERVATION_STANDALONE_BACKEND_LIVE_BASE_URL` plus optional `RESERVATION_STANDALONE_BACKEND_LIVE_HEALTH_PATH` (default `/v1/health`) and timeout env to prove a configured standalone backend health/readiness endpoint returns JSON 2xx; safe mode skips without a fully valid standalone URL and makes no network call. This is health/readiness proof only and does not prove database connectivity, RLS, tenant isolation, durable idempotency, SDK/direct parity, package install, optional chat, or compatibility route removal by itself. `backend-platform:live-proof-readiness` now runs local current frontend consumer repo readiness, compatibility route removal gate readiness, backend package graph boundary readiness, backend AI chat boundary readiness, backend extraction/source boundary readiness, backend extraction dry-run readiness, extracted backend workspace readiness, standalone API skeleton build/test readiness, and database migration bundle readiness before tying together standalone deployment config, standalone backend health proof, database live proof, SDK/direct live parity, and SDK registry install proof env parsers. These local prerequisites do not make network, database live-proof, registry, install, publish, deploy, browser, or live mutation calls. The strict readiness variant fails unless those local prerequisite gates pass and the strict proof commands are configured enough to run, including an explicit `RESERVATION_PLATFORM_LIVE_CHAT_MODE` so optional chat is not silently ignored. The extraction/source and chat boundary prerequisites are local source scanning only; they prevent Next/React/frontend/current-app/provider-runtime drift before any live proof is treated as ready. The database migration bundle prerequisite is local migration-index/manifest readiness only, not `database:live-proof`. The SDK/direct live parity command now has a disabled-chat mode that compares SDK and direct HTTP public `chat_module_disabled` status/body for all four contracted chat routes; enabled provider-backed live chat proof remains an unsupported/pending failure. `sdk:release-gate` still includes safe readiness and safe live health proof. This does not complete full live proof, create a frontend or backend repo, run extracted-repository install/build/test commands, delete compatibility routes, publish packages, install registry artifacts, or prove a database: actual disposable database migration application, RLS/tenant isolation, durable idempotency, SDK/direct live parity, package install, and enabled optional chat behavior remain incomplete until the strict commands pass against disposable live infrastructure. |
| Extract or release the backend platform as its own repository/service boundary. | [Phase 11 Backend Repository Extraction](phase-11-backend-repo-extraction.md) | Partially implemented as readiness/docs plus a bounded local materialization proof. Phase 11 now references the actual `standalone-backend-extraction-manifest.json` plus `backend-platform:verify-extraction-manifest` and `backend-platform:verify-extraction-dry-run` guardrails, and adds `backend-repo-bootstrap.md` plus `backend-package-ownership.md` for install, build/test, runtime env, database proof, live proof, SDK consumer integration, package visibility, consumer-safety, and excluded frontend concerns. `backend-platform:verify-package-graph-boundary` now locally checks the backend package/app `package.json` inventory, blocks frontend-only dependencies from backend-owned manifests, and keeps `@reservation-platform/sdk` free of backend-only runtime dependencies. `backend-platform:verify-extracted-workspace-readiness` now adds a CI-safe extracted-workspace metadata model proof over the manifest and package manifests, including package-root rename alignment, root/package script claims, extracted workspace dependency closure, frontend/current-app source exclusion, and SDK consumer-safety. `backend-platform:verify-extraction-dry-run` now copies only planned move/copy manifest entries into an OS temporary backend target tree candidate, generates backend-only root workspace metadata there only (`package.json`, `pnpm-workspace.yaml`, and a minimal `tsconfig.json`), validates backend-only materialized targets, generated root metadata, pinned pnpm workspace metadata, backend-safe root build/test dev tools (`@types/node`, `tsx`, and `typescript`), workspace globs, root scripts, generated `node scripts/*.mjs` references, known default input docs/manifests for those node commands, generated root `corepack pnpm --filter <package-name> run <script>` command coherence against exactly one materialized `apps/*` or `packages/*` package manifest plus that manifest's declared package script, package-level import-closure readiness for materialized package/app source against each package's own dependency metadata, including declared imports of other materialized workspace packages, Phase 11 aggregate script composition, applicable package manifests, and the backend-candidate source boundary directly against the materialized OS-temp tree, excludes compatibility-shim/excluded/generated/install/cache artifacts, and cleans the temp tree up by default unless the boolean keep-debug env is set. The generated backend root script set now keeps candidate-local source-boundary scanning, package build/test, standalone API skeleton, and database migration-index checks while leaving monorepo-only extraction/readiness/package-graph and migration-bundle reconciliation guardrails in the current repository; the manifest therefore plans the candidate-local source-boundary verifier, the migration-index script, and the migration-index default database migration bundle manifest for generated candidate root command coherence, while the dry run proves `backend-platform:verify-extraction-boundary`, `packages:build`, `packages:test`, and standalone API skeleton commands point at materialized scripts or packages that define the requested scripts, fails copied backend source with `materialized backend package dependency closure` diagnostics when it imports an undeclared external or workspace package, and also fails copied backend source with `materialized backend boundary:` diagnostics when it imports React, Next.js/current-app routes, frontend components, browser helpers, or frontend-only markers. Workspace package presence alone is not sufficient; the importing package must declare the workspace package dependency. Unit coverage also proves the missing-default-input guard catches the standalone extraction manifest path if an extraction verifier script is reintroduced. This is still an OS-temp generated backend workspace metadata, package import-closure, root-script/package-script, and source-scan coherence proof only: it does not create a separate permanent repository, mutate source files or git-tracked target paths, install dependencies, publish packages, deploy a backend, run actual extracted-repository build/test outside the current Next.js workspace, prove live database/RLS/idempotency behavior, prove live parity, or complete final repo extraction. Current frontend app code and compatibility route files remain excluded or reference-only, not canonical backend repo code. |
| Prove the current frontend can be separated into its own consumer repository. | [Phase 12 Frontend Repository Consumer Proof](phase-12-frontend-repo-consumer-proof.md) | Partially implemented as local inventory/readiness proof for reservation platform client/wrapper/proof source only. `frontend-consumer-repo-inventory.json` classifies the reservation form component closure, reservation platform client, browser-safe chat transport wrapper, public DTO types, admin reservation data helpers, and admin reservation loader as included proof source; backend platform internals as excluded; and route shells/navigation/admin UI, full chat UI, content, analytics, broad admin, landing, and app scaffolding areas as reference-only until their app-owned API and navigation dependencies are separated or deliberately included in a later proof. `current-frontend:consumer-repo-readiness` validates the inventory, listed paths/packages, browser-safe frontend env names, prerequisite boundary commands, included local source import closure, generated package metadata external import closure, and prevents backend-only packages/paths from being marked frontend runtime/include. The prerequisite boundary and secret scans now also use the inventory's `include` source areas as scan roots, so inventory expansion automatically enters the browser-safe/backend-import/secret checks while their broader migrated reference targets remain scanned. It also creates an OS-temp frontend consumer target-tree candidate from include entries only, writes generated private root `package.json` metadata from frontend-runtime, SDK-consumer, and frontend-dev inventory dependencies plus an exact pinned source-root `pnpm@x.y.z` `packageManager` and frontend-only `typecheck`, `build`, and `start` scripts, rewrites SDK-facing package dependencies away from root `workspace:*` specs to deterministic `0.0.0` placeholders, validates those scripts are present/non-empty, do not contain backend/current-monorepo/live-proof command fragments, and have command binaries backed by generated package metadata (`typescript` for `tsc --noEmit`, `next` for `next build` and `next start`), rejects generated metadata containing backend-only or current-monorepo-only dependency entries, rejects invalid package-manager values, rejects generated `dependencies` or `devDependencies` specs using `workspace:`, `file:`, `link:`, or `portal:`, checks materialized source external imports against generated `dependencies` and `devDependencies` while allowing Node built-ins and deriving scoped packages as `@scope/name`, writes generated root frontend-safe `tsconfig.json` metadata with DOM libs, strict/noEmit, bundler module resolution, React JSX, root `@/*` alias, source includes, and `node_modules` exclusion, rejects tsconfig `extends`, references/output settings/backend paths/backend packages/`packages` or `apps` path segments/extra aliases/absolute paths/`..` traversal/`.next` generated includes after backslash normalization, filters generated/install/cache artifacts, blocks backend/current-app server paths and workspace-root metadata such as `pnpm-workspace.yaml` or `turbo.json` in the copied tree, rechecks materialized local import closure, and cleans the temp tree up by default unless `CURRENT_FRONTEND_CONSUMER_KEEP_MATERIALIZED_TREE=1` is set for debugging. This is still a temporary target-tree and package/tsconfig metadata portability/workspace-metadata/script-dependency-coherence/import-closure proof for the selected wrapper/source slice only. A separate prepared-artifact strict install/typecheck/build proof now passes once from an external root, but this does not create a separated frontend repo, extract the full chat UI, delete compatibility routes, publish the SDK, prove registry install, run live browser checks, or prove a separated frontend runtime flow; those remain open. |
| Define the backend platform as the actual product repository. | [Phase 13 Backend Platform Product Repository](phase-13-backend-platform-product-repo.md) | Planned. This phase owns backend product ownership, backend-only CI/release gates, deployment/runtime configuration docs, package visibility rules, and compatibility route exclusion policy. It builds on Phase 11 but treats the backend repo as a product boundary, not just a copied file set. |
| Make the SDK a real installable consumer contract. | [Phase 14 SDK Release and Consumer Contract](phase-14-sdk-release-consumer-contract.md) | Planned. This phase owns SDK public contract docs, package export/dependency scans, clean external install proof, SDK/direct HTTP parity proof, versioning policy, and pack-inspection release guards. It must not publish without explicit approval. |
| Document operational readiness, deprecation, and release rules. | [Phase 15 Operations, Deprecation, and Release Readiness](phase-15-operations-deprecation-release.md) | Planned. This phase owns release checklists, rollback/deployment runbooks, compatibility deprecation policy, observability requirements, support matrix, and final remaining-gap closeout rules. It cannot treat skipped live checks as release proof. |
| Physically split the backend platform into its own GitHub repository boundary. | [Phase 16 Physical Backend Repository Split](phase-16-physical-backend-repo-split.md) | Planned. This phase owns the actual backend repository extraction plan, backend-only dry-run proof, clean clone bootstrap, backend package graph validation, and downstream updates when package ownership changes. It is the bridge from monorepo readiness to a real backend product repo. |
| Physically split the current frontend into a consumer repository. | [Phase 17 Physical Frontend Repository Split](phase-17-physical-frontend-repo-split.md) | Planned. This phase owns runnable frontend repo inventory, frontend-only package metadata, clean clone setup, build/smoke proof against an external `/v1` backend URL, and compatibility-route blocker updates. |
| Distribute the SDK as the external frontend integration contract. | [Phase 18 SDK Distribution and Contract](phase-18-sdk-distribution-and-contract.md) | Planned. This phase owns SDK public method contract, dependency/export scans, package tarball inspection, clean external install fixture, SDK/direct HTTP parity, and consumer quickstart docs. It does not publish without explicit approval. |
| Prove backend, SDK, and frontend work across separated repositories. | [Phase 19 Cross-Repo Release Proof](phase-19-cross-repo-release-proof.md) | Planned. This phase owns the final release proof chain across backend build/test, disposable database/RLS proof, backend deployment proof, SDK pack/install proof, SDK/direct HTTP live parity, frontend consumer build/smoke proof, and compatibility route removal gate. Skipped readiness checks do not count as release proof. |
| Make repository ownership unambiguous before the physical split. | [Phase 20 Separation Source of Truth](phase-20-separation-source-of-truth.md) | Planned. This phase creates the authoritative ownership matrix for backend-owned, frontend-owned, SDK-owned, shared-contract, compatibility-only, and reference-only source so later subagents do not copy files across boundaries just to make builds pass. |
| Materialize the backend repo candidate from the current workspace. | [Phase 21 Backend Repo Materialization](phase-21-backend-repo-materialization.md) | Planned. This phase generates or dry-runs an installable backend-only workspace from the extraction manifest and proves it can build/test without current frontend app files, compatibility routes, browser helpers, or UI dependencies. |
| Materialize the frontend repo candidate as a backend consumer. | [Phase 22 Frontend Repo Materialization](phase-22-frontend-repo-materialization.md) | Planned. This phase expands the frontend inventory into a runnable app candidate, proves the frontend can build against an external platform base URL, and records any remaining `/api` assumptions as compatibility blockers. |
| Materialize the SDK as the package users install from another app. | [Phase 23 SDK Package Materialization](phase-23-sdk-package-materialization.md) | Planned. This phase packs/installs the SDK and public contract packages into clean external fixtures, checks package artifacts for backend leakage, and proves SDK/direct `/v1` parity without workspace links. |
| Prove a new frontend can adopt the backend platform from outside this repo. | [Phase 24 Cross-Repo Adoption Proof](phase-24-cross-repo-adoption-proof.md) | Planned. This phase runs the full separated adoption proof: backend repo candidate, disposable database/live backend proof, SDK artifact install, external frontend fixture build/smoke, SDK/direct parity, and compatibility route removal decision log. |
| Define the backend GitHub repository as the product boundary. | [Phase 25 Backend Product Repository Contract](phase-25-backend-product-repo-contract.md) | Planned. This phase makes the backend repo contract explicit: public `/v1` API, SDK release policy, database/RLS/idempotency ownership, operations docs, and private backend internals. It blocks frontend-owned paths from backend product manifests and updates later phases when ownership changes. |
| Detach the current frontend so it behaves like a replaceable consumer. | [Phase 26 Frontend Consumer Detachment](phase-26-frontend-consumer-detachment.md) | Planned. This phase expands the frontend inventory into a true consumer app plan, blocks backend imports in frontend source, records every remaining `/api` dependency as compatibility-only, and turns missing behavior into backend or SDK requirements. |
| Make the SDK the public release surface for new frontends. | [Phase 27 SDK Public Release Surface](phase-27-sdk-public-release-surface.md) | Planned. This phase defines SDK public exports, consumer setup, package artifact inspection, clean install fixtures, SDK/direct HTTP parity, and version compatibility. It explicitly forbids backend implementation code, migrations, provider workflows, UI, and workspace-only references in SDK artifacts. |
| Prove the backend product and SDK from a clean external frontend. | [Phase 28 Live Backend and External Consumer Proof](phase-28-live-backend-and-external-consumer-proof.md) | Planned. This phase requires a standalone backend target, disposable database/live behavior proof, SDK package artifact install, external frontend smoke from outside the repo, SDK/direct parity against the same backend, and compatibility removal decisions based on evidence. Skipped readiness checks do not count. |
| Coordinate subagents around the repo-first plug-and-play outcome. | [Phase 29 Subagent Execution Matrix](phase-29-subagent-execution-matrix.md) | Planned. This phase defines worker focus, spec review gates, quality review gates, and downstream update rules for Phases 25 through 28 so subagents preserve the backend-as-product, SDK-as-install-surface, frontend-as-consumer model. |
| Make SDK and contract packages installable for a clean frontend proof. | [Phase 30 Package Source and Frontend Proof](phase-30-package-source-and-frontend-proof.md) | Partially implemented. A prepared-artifact strict frontend install/typecheck/build proof now passes from an external root using staged SDK and contract tarballs, and a disposable local registry strict install/type-import proof now passes for exact `@reservation-platform/sdk@0.0.0` and `@reservation-platform/contract-types@0.0.0` package specs. Public/private registry proof remains incomplete unless the release path requires one, so this phase still owns final package-source selection and downstream updates if package names, versions, or install source change. |
| Prove database ownership against disposable infrastructure. | [Phase 31 Disposable Database Proof](phase-31-disposable-database-proof.md) | Passed for disposable PostgreSQL database behavior on 2026-06-27. `database:live-proof:strict` applied all 11 backend-owned package migrations against the named disposable Docker Postgres container `reservation-proof-postgres-d8b0`, then verified booking RLS, public insert policy, anon catalog read, anon booking insert, non-admin authenticated booking invisibility, admin authenticated booking visibility, and durable platform idempotency claim/store/replay through database RPCs. This closes the database migration/RLS/idempotency proof slice, but does not prove standalone backend service runtime, SDK/direct live parity, or compatibility route removal. |
| Prove the backend as a standalone live service. | [Phase 32 Standalone Backend Live Proof](phase-32-standalone-backend-live-proof.md) | Passed for disposable DB-backed local route behavior, committed deployment config, backend-owned browser CORS preflight contract, and current-frontend public booking browser smoke on 2026-06-27. `backend-platform:db-backed-live-parity-proof:strict` applied package-owned migrations to `reservation-proof-postgres-d8b0-sdk`, verified database RLS/admin visibility/durable idempotency behavior, seeded a neutral service/resource fixture, started the standalone `apps/api` `/v1` backend with proof-only PostgreSQL-backed repository adapters, and served metadata, catalog, availability, reservation, disabled-chat, idempotency replay, and resource-maintenance behavior to the live parity verifier. `current-frontend:db-backed-platform-smoke:strict` separately started a disposable DB-backed standalone backend with frontend-origin CORS, started the current frontend on a separate origin in platform mode, and drove `/form-booking` through browser-observed `GET /v1/services`, `GET /v1/availability`, and `POST /v1/reservations` without current-frontend `/api` fallback. `apps/api/deployment.config.json` records the build/start/health/env/CORS contract, and `backend-platform:verify-standalone-deployment-config:strict` passes with complete backend-only env placeholders. Hosted deployment to a real platform target, permanent backend runtime PostgreSQL adapter ownership, admin DB-backed browser smoke, and fully materialized external-frontend browser smoke remain open if required before route cleanup. |
| Prove the SDK matches direct HTTP behavior against the standalone backend. | [Phase 33 SDK and Direct HTTP Parity Proof](phase-33-sdk-direct-parity-proof.md) | Passed for the DB-backed standalone disposable backend on 2026-06-27. The DB-backed proof invoked the existing strict SDK/direct parity verifier against the same local standalone `/v1` backend URL and passed metadata, service, resource, availability, reservation list/summary, disabled chat errors, reservation create idempotency replay, reservation read/list after create, resource-maintenance list/create idempotency replay/end idempotency replay/list after end. The generic `sdk:live-parity:strict` env surface, external frontend browser smoke, and compatibility route cleanup remain separate gates. |
| Prove registry or release-artifact installation from a clean consumer. | [Phase 34 Registry Release Proof](phase-34-registry-release-proof.md) | Passed for disposable local registry proof on 2026-06-27. `sdk:registry-install-proof:strict` with `RESERVATION_SDK_REGISTRY_PROOF_MODE=disposable`, exact SDK and contract package version specs, and `RESERVATION_SDK_REGISTRY_ALLOW_INSTALL=1` started a temporary npm-compatible registry, served packed SDK/contract-types plus local `zod` dependency tarballs, installed by package name/version into an external temp consumer, and typechecked SDK value plus contract type imports. Public/private registry pilot remains separate and requires explicit approval before publishing. |
| Decide compatibility route removal, deprecation, or retention from evidence. | [Phase 35 Compatibility Cleanup and Release Decision](phase-35-compatibility-cleanup-release-decision.md) | Planned. This phase owns rerunning compatibility route gates after frontend, database, backend, parity, and registry proofs; removing routes only when replacement paths are proven; and documenting deprecation or retained-route blockers with owners. |
| Prove migrations, row-level security, and tenant isolation in a disposable backend. | [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Database proof is now backed by a real disposable run. The package-owned migration bundle includes concrete verified assets for extensions, tenant/auth compatibility, catalog schema, resource schema, booking schema, resource-maintenance schema/RPC, availability-rule schema, atomic reservation RPC, RLS policies, core security hardening, durable idempotency SQL, a generated ordered checksum index that separates core, optional AI retrieval, and development seed inputs, package-local DB-client-neutral helpers/contracts for selecting deterministic migration plans from that index, and a Docker-capable live migration harness. `database:live-proof:strict` passed on 2026-06-27 against a disposable Docker Postgres container, applying the backend-owned plan and verifying RLS/admin visibility plus durable idempotency behavior. Live standalone backend connectivity, route-level auth/tenant behavior, SDK/direct parity, and compatibility route cleanup remain incomplete. |

## Phase 11 Backend Candidate Doc Materialization Note

Phase 10 live proof readiness now preserves the distinction between local
compatibility-route gate validity and actual route removability. The
compatibility prerequisite can report `ready` when its inventory, source scan,
standalone surface proof, and decision-log checks are internally valid, but its
readiness message also reports removable-route counts and strict prepared-root
proof blockers. Routes remain non-removable until the strict current-frontend
consumer install proof and strict extracted-backend install/build/test proof
are true for the relevant compatibility routes.

The Phase 11 backend repository extraction gap now includes a bounded OS-temp
dry-run proof that `backend-repo-bootstrap.md` and
`backend-package-ownership.md` are copied into
`docs/backend-platform-extraction/` in the backend candidate. Focused unit
coverage also proves frontend separation planning docs, especially
`frontend-backend-sdk-separation/frontend-backend-separation-completion-plan/README.md`,
are not materialized. This is still not a permanent backend repo, install,
publish, deploy, live infrastructure proof, or extracted-repository build/test
outside the current workspace.

Phase 11 now also includes `backend-platform:extracted-install-proof` and
`backend-platform:extracted-install-proof:strict` as the explicit prepared-root
install/build/test proof harness. The default command is CI-safe and reports
`SKIPPED` or `READY` without running install, network, publish, or generated
backend commands. The strict command fails closed unless
`RESERVATION_EXTRACTED_BACKEND_PROOF_ROOT` points at a prepared extracted
backend workspace outside the current repo and
`RESERVATION_EXTRACTED_BACKEND_PROOF_ALLOW_INSTALL=1` is set, then runs only
the static allowlisted `corepack pnpm install --frozen-lockfile --ignore-scripts` and
`corepack pnpm run phase-11:verify-generated-backend-workspace` sequence. The
install disables package and dependency lifecycle scripts while still running
the generated backend workspace verifier after install. That strict proof
passed once against
`C:\tmp\reservation-separation-proofs\standalone-backend-extraction-yBf9oq`.
The frontend strict proof initially blocked before lockfile generation when
the external frontend candidate resolved `@reservation-platform/contract-types`
and `@reservation-platform/sdk` from npm as `0.0.0`. A later prepared-artifact
proof passed from `C:\Users\User\AppData\Local\Temp\current-frontend-consumer-tree-3vrf7e\frontend-consumer`
using staged SDK and contract tarballs, but registry proof remains incomplete.

## Subagent Handoff Rule

When assigning a subagent a remaining gap, give it this index plus the owning
phase result file. The subagent should update both files when it completes or
changes the status of a gap.

If a later phase discovers that an earlier phase needs a new contract or
backend capability, update the earlier phase result first, then update this
index and the downstream phase result. This keeps the phase sequence stable and
prevents a later proof from silently depending on undocumented work.
