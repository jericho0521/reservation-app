# Remaining Modularity Gaps

This index answers whether the known remaining modularity work is captured in
the phase docs. It does not replace the phase plans; it points each gap to the
phase that owns it so subagents can work from one phase file without losing the
overall outcome.

## Gap Ownership

| Remaining gap | Owning phase doc | Current status |
| --- | --- | --- |
| Extract platform route handlers from Next.js route glue into backend-owned application services. | [Phase 1 Boundary Results](phase-1-backend-module-boundary-results.md) | Explicitly documented. Availability query preparation, repository read orchestration, slot generation, metadata/default-label handling, DTO shaping, and storage error result shaping now live in `@reservation-platform/api` behind an injected availability repository port. Catalog list/read orchestration for venues, services, resources, and resource layouts now runs through a framework-neutral `@reservation-platform/api` catalog request dispatcher behind an injected `PlatformCatalogRepository`; the current Next compatibility files wire the repository factory and JSON serialization only. Protected-route host Supabase auth preflight is centralized in `/api/v1` route utilities, and reservation list/read-by-id plus create/update/cancel/reschedule mutation orchestration now live in framework-neutral `@reservation-platform/api` application-service functions backed by injected repository ports. The Next compatibility files now own auth preflight, storage package factory wiring, create repository construction, resource-id label resolution for create/reschedule preparation, and JSON serialization for those paths. Resource-maintenance list/create/end orchestration also lives in framework-neutral package services, and `apps/api` now routes the standalone `GET /v1/resource-maintenance`, `POST /v1/resource-maintenance`, and `POST /v1/resource-maintenance/{id}/end` contract through an injected `ResourceMaintenanceRepositoryPort`. Reservation read and update-style mutation Supabase adapters now live in `@project-play/reservations-supabase`; auth/tenant enforcement and live standalone backend rollout remain incomplete. |
| Replace compatibility booking/seat naming with generic reservation/resource service contracts. | [Phase 1 Boundary Results](phase-1-backend-module-boundary-results.md) | Partially implemented. Public `@reservation-platform/api` reservation service error messages now use generic reservation/resource language for create validation, atomic capacity/resource conflicts, list/read failures, update failures, and cancel failures. Resource conflict/capacity details now include generic `resource_labels` and `available_quantity` aliases while preserving legacy `seat_labels` and `available_seats` for compatibility consumers. Legacy storage fields, compatibility adapter internals, fixtures, and DB naming remain until the final DB/API migration. |
| Move database access behind backend repository interfaces. | [Phase 1 Boundary Results](phase-1-backend-module-boundary-results.md) | Partially implemented. The current `/api/v1` catalog shim now wires `createSupabasePlatformCatalogRepository()` from `@project-play/reservations-supabase`, preserving the anon-client venue/service reads and service-role resource/layout reads while moving table/select/order/filter logic into the backend storage adapter package. Catalog endpoint matching, id decoding, `service_id` resource filtering, missing-repository failure shaping, and catalog DTO/error result shaping now live in `@reservation-platform/api`; Next.js route files only pass the request/path into shared serialization glue. A source-level tenant/venue repository adapter proof also exists there. `/api/v1/availability` now injects a neutral `AvailabilityRepositoryPort` into `@reservation-platform/api`; the local factory still wires `createSupabaseAvailabilityRepository()`, while the package service owns query validation, storage read orchestration, slot generation, metadata/default-label handling, platform DTO shaping, and platform error result shaping. Resource-maintenance list/create/end storage now delegates active-row listing, resource lookup, service lookup, legacy maintenance upsert, and end-update table/select details to `createSupabaseResourceMaintenanceRepository()`, and framework-neutral `@reservation-platform/api` service functions own the policy preparation, repository error classification, and DTO response shaping through a repository port. Reservation list/read-by-id orchestration now uses a framework-neutral `ReservationReadRepositoryPort`, and `createSupabaseReservationReadRepository()` owns the `bookings` `*, services(name)` select, descending `booking_date` order, search filter application, search-only limit, id equality, and `.single()` query shape in the storage package. Reservation update/cancel/reschedule mutation orchestration now uses a framework-neutral `ReservationMutationRepositoryPort`; package services own UUID validation, legacy update patch validation, `updated_at` stamping, cancel status construction, repository error classification, and platform DTO/error response shaping, while `createSupabaseReservationMutationRepository()` owns the authenticated Supabase `bookings.update(patch).eq("id", reservationId).select().single()` query shape in the storage package. Reservation create orchestration now uses a framework-neutral `ReservationCreateRepositoryPort`; the package owns legacy create validation, legacy booking-to-reservation conversion, atomic error classification, and platform DTO/error body shaping while current app compatibility glue wires `createSupabaseReservationRepository(supabaseAdmin())`. Reservation create/reschedule compatibility shims resolve resource ids to labels through `createSupabaseReservationResourceLabelRepository()` instead of spelling the `reservable_resources` label lookup in the route helper. `apps/api` now has a backend-only runtime factory that can wire catalog, availability, reservation read/create/mutation, resource-maintenance, idempotency, and tenant/venue context repositories from `RESERVATION_SUPABASE_URL`, `RESERVATION_SUPABASE_ANON_KEY`, and `RESERVATION_SUPABASE_SERVICE_ROLE_KEY` without importing current-app Supabase helpers. Live route-wide adoption, live DB/RLS proof, deploy proof, and final standalone backend extraction remain incomplete. |
| Add bearer-token-aware platform auth and tenant/venue enforcement. | [Phase 4 Auth and Config Split Results](phase-4-auth-tenant-runtime-config-split-results.md) | Partially implemented. Current frontend and SDK forward context headers, and `@reservation-platform/api` now parses them framework-neutrally, provides framework-neutral auth/tenant decision helpers for already-verified principals, includes a provider-neutral `principalFromTokenClaims()` mapper for decoded token claims, and includes a repository-backed tenant/venue validation helper. `@project-play/reservations-supabase` now has a source-proofed `createSupabaseTenantVenueRepository(client)` adapter that reads the planned `tenants` table and selects `id, tenant_id` from `venues` for fail-closed ownership validation. The current `/api/v1` route utility layer now has a backend-only `RESERVATION_PLATFORM_SERVICE_API_KEY` service-token preflight adapter that consumes `Authorization: Bearer ...` through the package parser and can reject before body parsing/idempotency mutation work; existing protected mutation shims now centralize their current host Supabase cookie/session preflight there too. Admin client components now use `lib/admin-auth-client.ts` for sign-in/sign-out only, and current-frontend boundary scans block direct raw Supabase browser helper imports outside that app-owned auth facade. Current protected reservation/resource-maintenance mutation shims validate required `X-Reservation-Tenant-Id` plus optional `X-Reservation-Venue-Id` through the Supabase tenant/venue repository after current host auth and before body parsing/mutation execution, and reservation read/list compatibility shims now run the same host-auth tenant/venue validation before reservation read storage work. `apps/api` now separately supports optional standalone backend service-token protection and provider-neutral JWT/JWKS bearer verification from standalone runtime env for catalog, availability, reservation, and resource-maintenance data routes. The service token remains a server-to-server bypass when it exactly matches; non-service bearer tokens can be verified through the JWT/JWKS verifier. Verifier principals are fed into the shared tenant/venue/role/scope authorization helpers, verifier failures return stable platform errors, thrown verifier errors fail closed without provider details, and auth still runs before route repository, idempotency, and protected mutation body work. Metadata and disabled chat remain unprotected. This proves standalone provider-neutral JWT/JWKS verification, bounded JWKS cache/unknown-`kid` refresh behavior, authorization plumbing, and decoded-claim mapping. Live provider configuration, provider operational key rotation, live DB migration/application proof, tenant/RLS proof, broad live route wiring, deployment, and final standalone backend extraction remain missing. |
| Add idempotency persistence, replay, and misuse protection in the backend. | [Phase 4 Auth and Config Split Results](phase-4-auth-tenant-runtime-config-split-results.md) and [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Partially implemented. SDK/current frontend forward `Idempotency-Key`, backend context parsing can read it, and `@reservation-platform/api` now has a framework-neutral idempotency repository port with atomic claim semantics, canonical JSON fingerprint helper, proceed/replay/misuse/in-progress decision semantics, and commit helper. Current reservation and resource-maintenance `/api/v1` compatibility mutation shims now use route-level begin/replay/commit wiring backed by a backend-runtime resolver that selects the durable Supabase idempotency repository when service-role config is present and falls back to in-process local/dev/test compatibility storage when config is absent. `apps/api` now proves idempotent `POST /v1/reservations`, reservation lifecycle mutation host wiring, `POST /v1/resource-maintenance`, and `POST /v1/resource-maintenance/{id}/end` with an injected `IdempotencyRepository`, required `Idempotency-Key`, malformed-JSON preparse rejection when the key is missing, completed-response replay, key-misuse rejection, and no completed idempotency record for invalid maintenance bodies using fake repositories. Its backend-only Supabase runtime factory wires `createSupabaseIdempotencyRepository()` when complete standalone Supabase config is present. `@project-play/reservations-supabase` now has a backend-only Supabase idempotency repository adapter, and `packages/database/migrations/supabase/000011_platform_idempotency.sql` now exists as the concrete package-owned bundle asset for the `platform_idempotency_records` table plus durable idempotency claim/store RPCs. Disposable database proof, migration application, tenant/RLS proof, live durable idempotency behavior, and live standalone rollout remain incomplete. |
| Keep SDK frontend-safe and HTTP-only. | [Phase 2 SDK Boundary Results](phase-2-sdk-boundary-public-client-results.md) | Explicitly documented and partially implemented. Package boundary scans exist, but registry/private-package install proof is still part of the release gate. |
| Make the current frontend a consumer of the backend API/SDK instead of backend owner. | [Phase 3 Frontend API Migration Results](phase-3-frontend-api-migration-results.md) | Explicitly documented and partially implemented through `lib/reservation-platform-client.ts`. The server-rendered `/admin` initial reservation load now calls a server loader that delegates to the wrapper instead of querying `bookings` directly; platform mode now uses the optional `GET /v1/reservations` summary contract for exact `confirmed_today` counts when available and falls back to deriving from returned bookings only for compatibility responses without summary. Some local compatibility routes remain intentionally in place. |
| Move LangChain/provider/retrieval workflow behind backend chat services. | [Phase 5 AI Chat Split Results](phase-5-ai-chat-workflow-split-results.md) | Partially implemented. SDK chat namespace and disabled/enabled fixture proofs exist, the current `/api/v1` compatibility surface now returns provider-free `chat_module_disabled` responses for the contracted disabled chat endpoints including the portable `messages:stream` route representation, and the standalone backend extraction manifest now explicitly plans `packages/ai-chat` as the backend-owned optional chat package while keeping `packages/reservation-chat-core` as reference-only migration context. `apps/api` now has an optional provider-neutral `StandaloneApiChatModule` injection boundary for `POST /v1/chat/reservation-sessions`, `/messages`, `/messages:stream`, and `/confirm`; absent injection preserves the exact disabled platform error body, injected fake modules receive tenant/venue/auth/correlation/idempotency request context, enabled responses can flow through all four routes, and thrown or returned module errors are sanitized. Real provider adapters, live retrieval, checkpoint persistence, live chat configuration, durable tenant-isolation enforcement, deployment wiring, and seeded live backend parity remain missing. |
| Prove a clean external frontend can install/use the SDK against `/v1`. | [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Explicitly documented and partially proven with fixtures. Strict live parity readiness now covers reservation create/read/list idempotency replay plus resource-maintenance list/create/end SDK/direct HTTP replay when strict env and mutation opt-in are configured. Live seeded backend parity is still unproven until that strict command passes against disposable seeded backend data; package publication proof also remains missing. |
| Remove local compatibility API routes. | [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Explicitly documented as blocked until backend parity, frontend migration, and external proofs pass. |
| Split the backend platform into its own repository or deployable service. | [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Partially implemented as a bounded skeleton proof. `apps/api` is now a private backend-only workspace package with framework-neutral route handlers, an optional Node HTTP server, tests for `GET /v1/metadata`, catalog `GET /v1/venues`, `/v1/services`, `/v1/resources`, `/v1/resource-layouts/{id}` through an injected `PlatformCatalogRepository`, `GET /v1/availability` through an injected `AvailabilityRepositoryPort`, read-only `GET /v1/reservations` and `GET /v1/reservations/{id}` through an injected `ReservationReadRepositoryPort`, idempotent `POST /v1/reservations` through injected `ReservationCreateRepositoryPort` plus `IdempotencyRepository`, idempotent lifecycle mutations through injected `ReservationMutationRepositoryPort` plus `IdempotencyRepository`, resource-maintenance list through an injected `ResourceMaintenanceRepositoryPort`, idempotent resource-maintenance create/end through injected `ResourceMaintenanceRepositoryPort` plus `IdempotencyRepository`, optional service-token/context enforcement plus provider-neutral JWT/JWKS bearer verification for protected data routes, optional provider-neutral injectable chat module routing for `/v1/chat/reservation-sessions/**` with exact disabled fallback behavior, missing repository behavior, backend-only Supabase runtime config validation/wiring with fake-client tests including tenant/venue repository wiring, and source-boundary scans proving it avoids frontend/Next/React/browser Supabase/LangChain/provider/current-app wrapper imports. `packages/database` now also exists as a private database bundle package with concrete verified core schema, tenant/auth, resource-maintenance, availability-rule, atomic reservation RPC, RLS, idempotency migration target files, a generated migration-index checksum/apply-plan artifact for extraction, DB-client-neutral TypeScript helpers/contracts for selecting core, optional AI retrieval, and development seed migration plans from that index, and a package/root CI-safe `database:live-proof` harness that can apply the backend-owned migration-index plan to a disposable PostgreSQL database when env plus `psql` are configured. The standalone extraction manifest/dry run now explicitly includes both `packages/ai-chat` and `packages/database` as direct backend package candidates while older root/package SQL files remain reconciliation/reference inputs mapped by the SQL inventory and migration bundle manifests rather than dry-run copy sources. This is not live reservation parity, durable DB-backed idempotency proof against a real database, live provider configuration, provider operational key rotation, a completed database migration/RLS behavior proof, deployment, or an actual separate repository extraction. |
| Prove migrations, row-level security, and tenant isolation in a disposable backend. | [Phase 6 Proof and Removal Gate Results](phase-6-external-frontend-proof-removal-gate-results.md) | Explicitly documented as required live proof. The package-owned migration bundle now includes concrete verified assets for extensions, tenant/auth compatibility, catalog schema, resource schema, booking schema, resource-maintenance schema/RPC, availability-rule schema, atomic reservation RPC, RLS policies, core security hardening, durable idempotency SQL, a generated ordered checksum index that separates core, optional AI retrieval, and development seed inputs, package-local DB-client-neutral helpers/contracts for selecting deterministic migration plans from that index, and a CI-safe live migration harness. `corepack pnpm run database:live-proof` validates disposable DB env and safely skips when unconfigured; `database:live-proof:strict` / `RESERVATION_DATABASE_LIVE_STRICT=1` fails on missing/malformed env or unavailable `psql`, and applies the package-owned plan when fully configured. This is readiness only. A real disposable database run, seeded proof, RLS behavior assertions, tenant isolation assertions, live atomic reservation behavior, and live durable idempotency remain incomplete in this branch. |

## Subagent Handoff Rule

When assigning a subagent a remaining gap, give it this index plus the owning
phase result file. The subagent should update both files when it completes or
changes the status of a gap.

If a later phase discovers that an earlier phase needs a new contract or
backend capability, update the earlier phase result first, then update this
index and the downstream phase result. This keeps the phase sequence stable and
prevents a later proof from silently depending on undocumented work.
